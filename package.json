{
  "name": "andrej-karpathy-skills",
  "version": "1.0.0",
  "private": true,
  "description": "A static unofficial tribute page mapping Andrej Karpathy's public skill themes.",
  "scripts": {
    "assets": "python3 scripts/generate_brand_assets.py",
    "serve": "python3 -m http.server 4174 --bind 127.0.0.1",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
